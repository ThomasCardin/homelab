existingSecret: "litmus-admin-secret"

adminConfig:
  VERSION: "3.19.0"
  SKIP_SSL_VERIFY: "false"
  DB_SERVER: "mongodb://mongodb-0.mongodb-service.litmus.svc.cluster.local:27017/litmusdb?replicaSet=rs0"
  DB_PORT: "27017"
  DB_USER: ""  # From existingSecret
  DB_PASSWORD: ""  # From existingSecret
  ADMIN_USERNAME: ""  # From existingSecret
  ADMIN_PASSWORD: ""  # From existingSecret

ingress:
  enabled: true
  name: litmus-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
  ingressClassName: nginx
  host:
    name: litmus.ninebasetwo.net
    frontend:
      path: /
      pathType: Prefix
  tls:
    - secretName: litmus-tls
      hosts:
        - litmus.ninebasetwo.net

# MongoDB disabled - using external MongoDB 
mongodb:
  enabled: false

portal:
  frontend:
    replicas: 1
    resources:
      requests:
        memory: "150Mi"
        cpu: "125m"
      limits:
        memory: "512Mi"
        cpu: "550m"
  server:
    replicas: 1
    graphqlServer:
      resources:
        requests:
          memory: "250Mi"
          cpu: "225m"
        limits:
          memory: "712Mi"
          cpu: "550m"
    authServer:
      replicas: 1
      resources:
        requests:
          memory: "250Mi"
          cpu: "225m"
        limits:
          memory: "712Mi"
          cpu: "550m"