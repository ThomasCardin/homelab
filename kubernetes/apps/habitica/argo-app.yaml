apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: habitica
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: habitica
  source:
    repoURL: https://github.com/ThomasCardin/homelab.git
    path: kubernetes/apps/habitica/base
    targetRevision: HEAD
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
